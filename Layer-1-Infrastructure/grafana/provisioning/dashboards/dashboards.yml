apiVersion: 1

providers:
  - name: 'universal-db-dashboards'
    orgId: 1
    folder: 'Universal DB'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
